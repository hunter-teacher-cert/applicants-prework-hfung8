When you use printf, the Java compiler does not check your format string.
See what happens if you try to display a value with type int using \%f.

We get an illegal format conversion exception if an int is used with \%f. 

And what happens if you display a double using \%d?

We get the same message stating that an illegal formatc conversion exception has been made. 

What if you use two format specifiers, but then only provide one value?

We get a message "Exception in thread "main" java.util.MissingFormatArgumentException: Format specifier '%d'"